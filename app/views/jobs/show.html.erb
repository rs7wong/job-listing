<table class="table table-boldered custab table-hover">
  <thead>
    <tr>
        <td><%= @job.title %></td>
    </tr>
  </thead>
    <tr>
        <td><%= simple_format(@job.description) %></td>
    </tr>

</table>

<div class="text-center">
  <%= link_to("投交履歷", new_job_resume_path(@job), :class => "btn btn-default" ) %>
</div>
